import type { NumberFormatOptions } from './types';
export default function useNumberFormat({ locales, format, currency, currencyDisplay, unit, unitDisplay, signDisplay, groupDisplay, minimumIntegerDigits, maximumIntegerDigits, minimumFractionDigits, maximumFractionDigits, }?: NumberFormatOptions & {
    locales?: Intl.LocalesArgument;
}): import("react").MutableRefObject<HTMLInputElement>;
